export const SOCIAL_CONSTANTS = {
  // Content length limits
  COMMENT_MAX_LENGTH: 5000,
  POST_MAX_LENGTH: 2000,

  // Pagination defaults
  DEFAULT_PAGE_LIMIT: 20,
  FEED_PAGE_LIMIT: 50,

  // Notification aggregation
  NOTIFICATION_AGGREGATION_WINDOW_HOURS: 1,
} as const;
