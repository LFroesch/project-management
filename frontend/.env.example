# ========================================================================
# REQUIRED ENVIRONMENT VARIABLES - Dev Codex Frontend
# ========================================================================
# Copy this file to .env or .env.local and fill in your actual values
# NEVER commit .env files to version control!

# ========================================================================
# BACKEND CONNECTION (Required for development)
# ========================================================================
# Backend API URL
# Development: http://localhost:5003/api
# Production: https://yourdomain.com/api or leave empty to use proxy
VITE_API_URL=http://localhost:5003/api

# ========================================================================
# STRIPE BILLING (Optional - only if not self-hosted)
# ========================================================================
# Stripe publishable key for payment processing
# Only needed if running hosted version with billing
# If self-hosting, you can skip this
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_... (use pk_live_... for production)

# ========================================================================
# SENTRY ERROR TRACKING (Optional but recommended)
# ========================================================================
# Get these from your Sentry project settings
# https://sentry.io/
# VITE_SENTRY_DSN=https://...@sentry.io/...
# VITE_SENTRY_ENVIRONMENT=development
